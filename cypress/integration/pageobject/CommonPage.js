import basePage from "./BasePage";

export default class commonPage extends basePage {

    clickButton(button) {
        super.clickButton(button)
    }

    isNotVisibleElement(modalName) {
        super.isNotVisibleElement(modalName)
    }
}